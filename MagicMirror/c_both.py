import subprocess

# 누끼따는 코드
subprocess.run(['python3', 'image.py'], check=True)

# 색상 추출 코드
subprocess.run(['python3', 'color.py'], check=True)

#데이터베이스 삽입 코드
subprocess.run(['python3', 'clothes_up.py'], check=True)